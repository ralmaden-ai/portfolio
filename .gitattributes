# Normalise text files to LF in the repository so diffs stay clean
# regardless of which OS edits them.
* text=auto eol=lf

# Binary assets — never touch these
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.pdf binary
*.docx binary
